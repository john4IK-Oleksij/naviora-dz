[project]
name = "naviora-dz"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Aleks", email = "alosi4ek@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pydantic>=2.13.5",
]

[project.scripts]
naviora-dz = "naviora_dz:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
